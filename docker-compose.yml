services:
  schrodinger-portal:
    build: .
    container_name: schrodinger-portal
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
